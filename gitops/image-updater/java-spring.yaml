apiVersion: argocd-image-updater.argoproj.io/v1alpha1
kind: ImageUpdater
metadata:
  name: java-spring
  namespace: argocd
spec:
  # image + write-back config comes from the argocd-image-updater.argoproj.io/*
  # annotations on the java-spring Application (gitops/registry/app-java.yaml),
  # since useAnnotations makes the CR ignore writeBackConfig/images here.
  applicationRefs:
    - namePattern: "java-spring"
      useAnnotations: true
