apiVersion: argocd-image-updater.argoproj.io/v1alpha1
kind: ImageUpdater
metadata:
  name: dotnet-todoapi
  namespace: argocd
spec:
  writeBackConfig:
    method: "git:secret:argocd/image-updater-git-creds"
    gitConfig:
      repository: "https://github.com/asfcjr/dd-stack.git"
      branch: "main"
      writeBackTarget: "helmvalues:../values/dotnet.yaml"
  applicationRefs:
    - namePattern: "dotnet-todoapi"
      useAnnotations: true
